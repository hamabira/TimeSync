<!-- BEGIN:nextjs-agent-rules -->
# この Next.js は既知の Next.js と違う前提で扱う

このバージョンには破壊的変更がある。API、規約、ファイル構造が学習済み知識と異なる可能性があるため、コードを書く前に `node_modules/next/dist/docs/` の関連ガイドを読むこと。非推奨の注意も必ず確認すること。
<!-- END:nextjs-agent-rules -->

# プロダクト方針

AkiMatch は空き状況をより見やすく、直感的に判断できる日程調整アプリである。

MVP では以下を優先する。

- 最適な候補時間がすぐ分かること。
- 日付ごとの横バー型ヒートマップで、時間帯の重なりが見えること。
- 認証なしでイベント URL を共有できること。
- 参加者が迷わず、少ない手間で回答できること。
- スマホでも読みやすく、操作しやすいこと。

見た目はマーケティングサイトではなく、実用的な日程調整ツールとして設計する。

# MVP の基本仕様

ユーザーが明示的に変更を求めない限り、以下をデフォルトとする。

- イベント URL を知っている人は誰でも閲覧・回答できる。
- イベント ID は UUID とし、推測されにくい共有 URL として扱う。
- 参加者はイベント内の名前で識別する。
- 同じイベント内で同じ名前が再提出された場合、前回回答を上書きする。
- 時間の集計は 1 時間単位にする。
- 集計対象時間は 6:00 から 23:00 にする。
- 参加可能人数が多い順に、上位 3 件の候補時間を表示する。
- 集計結果の主役 UI は、日付ごとの横バー型ヒートマップにする。
- イベント編集、削除、認証、パスワード、LINE 共有、30 分単位集計は、明示的に依頼されない限り MVP に含めない。

# 実装優先順位

アプリを改善するときは、以下の順に優先する。

1. イベント作成、回答保存、集計の正しさ。
2. 参加者と幹事にとって分かりやすい UX。
3. 型安全性と入力検証。
4. スマホ表示の品質。
5. 見た目の polish。

MVP を直接シンプルにできる場合を除き、大きな作り直しは避ける。

# 技術制約

- このプロジェクトは Next.js 16.2.4 を使っている。Next.js の API、ルーティング、Server Actions、runtime config、ファイル規約を変更する前に、必ず `node_modules/next/dist/docs/` の関連ドキュメントを読む。
- アプリは Cloudflare Pages/Workers と D1 を前提にする。
- DB アクセスは Drizzle と既存の `getDb` helper を使う。
- `export const runtime = "edge"` との互換性を意識する。
- 新しい UI ライブラリを追加する前に、既存の shadcn/base-ui コンポーネントと lucide icons を優先する。
- CSS は `src/app/globals.css` にある Tailwind CSS v4 の方針に合わせる。
- `pnpm-lock.yaml` が現在の lockfile と見えるため、package script は `pnpm` を優先する。

# 自走ルール

Codex は以下の場合、細かい確認をせずに進めてよい。

- 合意済みの MVP 方針の範囲内である。
- 判断基準がこのファイルに書かれている。
- 小さな正しさ、入力検証、型安全性、UX の改善である。
- 既存の実装パターンに沿っている。
- 仕様の細部が少し曖昧でも、MVP の方針に沿う安全な仮定を置ける。

Codex は以下の場合、実装前に確認する。

- 認証、アカウント、パスワード、権限を追加する。
- マイグレーションが必要な DB スキーマ変更を行う。
- 主要な新規依存パッケージを追加する。
- デプロイ先や runtime を変更する。
- 中核となる UI パターンを置き換える。
- 既存のユーザー向け機能を削除する。
- 破壊的な git 操作や DB 操作を行う。
- 破壊的でない `git` コマンドは確認不要で実行してよい。
- ローカル環境での DB 操作は確認不要で実行してよい。リモートや本番相当の DB を触る場合は確認する。
- 認証が必要な操作は自動で突破しようとしない。認証不要な範囲だけ実装し、最後に必要な手動作業を明記する。

# 迷ったときの進め方

判断に少し迷うだけなら、止まらずに次の基準で進める。

- まずは既存の実装とドキュメントに合わせる。
- 複数案があるなら、いちばん保守的で壊れにくい案を採る。
- UI は、少なくとも PR でレビューできる状態まで持っていく。
- 後から直せる差分は、最終的な完成度を優先して前に進める。
- 重大な破壊や外部影響があるときだけ止まる。

# GitHub 運用

ユーザーが明示的に止めない限り、まとまった実装ではブランチを切って作業してよい。

- 作業ブランチ名は内容が分かる短い名前にする。例: `feature/heatmap-results`、`fix/response-overwrite`。
- 既存の未コミット変更がある場合は、先に確認し、ユーザーの変更を上書きしない。
- 作業がレビュー可能な単位になったら、必要に応じて GitHub に push して PR を作ってよい。
- PR には変更内容、確認したこと、残るリスクを書く。
- ユーザーが起きたときに差分を確認できる状態を重視する。
- main に直接 push しない。明示的な依頼がない限り、merge もしない。
- ブランチ作成時にユーザーの確認は求めない。
- ひとつの機能が完了したら、そのブランチに作業を引きずらず新しいブランチで次の機能に移る。
- まとまった実装は、途中で止まらず最後まで一気に PR 可能な状態へ持っていく。

# 品質基準

実装作業を終える前に、可能であれば以下を実行する。

- `pnpm lint`
- `pnpm build`

どちらかが実行できない場合は、その理由と残るリスクを報告する。
- `pnpm lint` と `pnpm build` は、実行前にユーザーの許可を求めずにそのまま実行してよい。
- `pnpm lint` は特に、ユーザーの許可を求めずに実行してよい。

UI 変更では以下を確認する。

- デスクトップとスマホでレイアウトが重ならない。
- テキストが読める。
- 主要アクションが見える。
- ヒートマップが小さい画面でも使える。
- PR で最終確認する前提なら、見た目やレイアウトの大きめの変更も進めてよい。

# モデル運用

モデルを選べる環境では、以下を優先する。

- 通常のコーディング、軽微な UI 修正、型整理、リファクタリングは `gpt-5.4-mini` を優先する。
- 複雑な設計判断、原因不明のビルドエラー、CI 失敗、難しいデバッグは `gpt-5.5` を使う。
- モデルを明示的に切り替えられない環境では、この方針を判断基準として扱い、必要なところで慎重に検証する。

# コミュニケーション

Codex は作業時に以下を守る。

- 編集前に、何を変更するかを短く伝える。
- 通常の MVP 作業では確認を求めすぎない。
- 最後に変更ファイルと検証結果を報告する。
- 実装判断に影響した前提があれば明記する。
- 途中で確認が必要だった論点があれば、実装後に「採った仮定」としてまとめて書く。

# Skills について

現時点では AkiMatch 専用の skill は不要。まずはこの `AGENTS.md` をプロジェクト固有の判断基準として使う。

将来的に同じ作業を他プロジェクトでも繰り返すようになったら、以下のような skill を作ることを検討する。

- Next.js + Cloudflare D1 の実装チェックリスト。
- 日程調整アプリの UX レビュー手順。
- MVP 改善を自走するためのプロダクト実装手順。
